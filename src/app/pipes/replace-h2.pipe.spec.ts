import { ReplaceH2Pipe } from './replace-h2.pipe';

describe('ReplaceH2Pipe', () => {
  it('create an instance', () => {
    const pipe = new ReplaceH2Pipe();
    expect(pipe).toBeTruthy();
  });
});
